function areYouPlayingBanjo(name) {
  // Implement me
  let lowerCasedName = name.toLowerCase()
  if(lowerCasedName[0] === 'r'){
  return `${name} plays banjo`}
  
  else {return `${name} does not play banjo`}
}